// import { TestWindow } from '@stencil/core/testing';
import { GraphOverview } from './graph-overview';

describe('GraphOverview', () => {
    it('should build', () => {
        expect(new GraphOverview()).toBeTruthy();
    });
});
