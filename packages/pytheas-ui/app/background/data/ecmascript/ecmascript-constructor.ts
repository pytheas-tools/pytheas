import { PyElement } from '../py-element';
import tsAstFinder, { NodeObject } from '../ts-ast-finder';

export class ECMAScriptConstructor extends PyElement {
    constructor(propertyNode: NodeObject) {
        super();
    }
}
