// import { TestWindow } from '@stencil/core/testing';
import { Graph } from './graph';

describe('Graph', () => {
    it('should build', () => {
        expect(new Graph()).toBeTruthy();
    });
});
